<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GroupProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:background">#fff</item>
    </style>

    <style name="Theme.GroupProject" parent="Base.Theme.GroupProject" />
    <style name="BtnStyle">
        <item name="android:backgroundTint">#28536B</item>
        <item name="android:fontFamily">@font/oswald</item>
        <item name="android:layout_width">match_parent</item>
    </style>

    <style name="HeaderStyle">
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">50dp</item>
        <item name="android:fontFamily">@font/oswald</item>
        <item name="android:textSize">45dp</item>
        <item name="android:textColor">#28536B</item>
    </style>
</resources>
